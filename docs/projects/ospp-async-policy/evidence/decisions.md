# 下一阶段决策记录

2026-09-16；代码基线 `a35a580`。

| 决策 | 当前状态 | 理由 / 待确认 |
|---|---|---|
| U7 采用 A，保留独立 experimental bridge | 本地实施选择 | 稳定 backend 和 coordinator 各有一份版本与准入状态，不能未经设计叠加；没有维护者同意记录。 |
| 默认复用上游 GRPO；offpolicy 仅为显式选择的消融 | 已实施，质量待验证 | 遵守 R3/R7；lag 不选择目标。尚未向导师发送建议。 |
| 不立即注册 async-grpo | 本地实施选择 | 官方允许 experimental class；公开 Trainer 初始化会启动 backend，需先确定生命周期和配置通道。 |
| Q1 独立控制训练与评测长度 | 实验入口已准备 | 消除截断归因中的直接混杂；实际样本顺序另行记录。 |
| P4 固定 lag=4 | 实验入口已准备 | 避免 C=4 被 lag=1 提前同步，确保 C 设置有实际区别。 |
| 启动失败回收采用独立、共享的 5 秒预算 | 已实施，待接口评审 | 到达 readiness deadline 后仍需确认子进程退出；语义已写入 docstring。 |
| 只准备本地 review 分支与沟通草稿 | 已授权的工程准备 | 用户没有明确要求发送消息；OSPP 中选及 PR 发布时间未确认。 |
| GPU 实验 | 待用户资源/预算答复 | 当前仅一张本机 GPU；AGENTS.md 要求运行 GPU 训练前确认。 |
| OSPP 状态 | 未确认 | 2026-09-14 资料记录为准备申请；本轮不能据此推断当前系统状态。 |

维护者对 B/C、算法入口、接口草案的意见仍为空；不得把上表本地判断写成已获批准。
